import customtkinter as ctk

ctk.set_appearance_mode("light")
ctk.set_default_color_theme("blue")

app = ctk.CTk()
app.title("Pricespy")
app.geometry("900x600")

#search bar
search_frame = ctk.CTkFrame(app)
search_frame.pack(pady=40)

#entryfield
search_entry = ctk.CTkEntry(search_frame, width=300, height=35, placeholder_text="Search products...")
search_entry.pack(side="left", padx=(0, 10))

# Search button
search_button = ctk.CTkButton(search_frame, text="Search", command=perform_search)
search_button.pack(side="left")
